# DropAgent — 需求描述

- **状态**：产品名已定（2026-09-05）。
- **文档性质**：需求描述。不是完整 PRD，不是技术方案，不是实现计划。
- **产品名**：DropAgent
- **品类**：菜单栏上的 Agent + Shelf 便捷小工具。不是工作台、不是指挥中心、不是 IDE。
- **本文管什么**：产品是什么、给谁用、主循环、做与不做、安全承诺怎么对外说、原型验什么。

---

## 1. 这是什么

把任意内容拖到菜单栏，可以先放着；选定动作后，交给用户**已经安装**的本地 Agent，在一次性副本里处理，结果作为**新文件**回到菜单栏，再由用户拖走或复制。

原件不被 DropAgent 覆盖。

一句话：

> DropAgent 是菜单栏小工具：先把内容放到 Shelf 上，再交给本机已装的 Agent 处理副本，结果拖走。原件不动。

DropAgent **不是**：

- 剪贴板管理器
- 纯置物架（Yoink / Dropover 已占住）
- AI 剪贴板（Stash / Clippy 已有总结、翻译、去背景）
- Agent 启动器（GroAsk 已能从菜单栏拉起多个 CLI）
- 去解析或遥控任意终端画面的通用 TUI 控制器（只把这一次投递打进对应 Agent 的会话）

---

## 2. 给谁用

第一版只服务这一类人：

**已经在本机安装并登录了 Grok、Codex、Claude Code 或 Gemini CLI 的人。**

他们真正烦的是：这些 Agent 按代码仓库干活，但手头只是一份 PDF、一张截图、一个链接，不想 `cd` 进某个项目，也不想让 Agent 碰到原文件。

DropAgent 给这些 Agent 一个**项目之外的投递口**。

没装任何可用终端 Agent 时：不提供云端 API，不提供 Ollama。界面说明「未发现终端 Agent」，头上芯片可选已装 TUI 或去安装。快捷动作跟芯片走：用当前所选 CLI 在任务副本里执行。没装对应 CLI、或该 CLI 没有无界面 / 可收口执行入口时，动作禁用并说清楚；有 TUI 仍可发送。禁止把动作 Prompt 拼进终端对话框完事，禁止解析 TUI 或模拟键盘去点菜单。

---

## 3. 主循环

```text
任意应用中的文件 / 图片 / 文字 / URL
        ↓
也可稍后用快捷键打开托盘，或在浏览器里用快捷键加入当前页
        ↓
进入待处理任务（此时可以先不跑）
        ↓
用户选择一个 Recipe（助理动作）
        ↓
内容复制到一次性任务工作区
        ↓
调用本机已装 Agent（只处理副本）
        ↓
结果作为新 Artifact 进下面的结果区，上面的文件还在
        ↓
点上面的文件继续对原件操作；再点取消选中；双击用系统默认方式打开。点下面结果区预览、复制、拖走产出
```

打开菜单栏后的**第一眼**是待处理任务，不是复制历史。第一次打开若抓页权限还没齐，会叠一张可跳过的就绪清单（终端、快捷键、辅助功能、已装浏览器的控制权）；点「以后再说」就进架子。拖文件不需要先走完这张卡。

复制历史只作为进货旁路：藏在文件行粘贴按钮后面，不是产品表面。⌘V 仍直接贴当前。

---

## 4. 已锁定的三项

| 决策 | 选择 | 含义 |
|------|------|------|
| 拖入后是否必须立刻跑 | 可以先放着 | 先放进 Shelf，稍后再选动作；不是 Dropzone 那种「放下就必须跑」 |
| 没装 Agent 怎么办 | 只服务已装 CLI 的人 | 引导安装芯片所选 CLI；第一版不接云端 API / Ollama |
| 默认安全档 | 分 Agent 说实话 | 见第 6 节。第一版不上容器 / 虚拟机 |

另外固定：

- 发现的是本机已装的 Agent。面板左文件列表、右 AI 区（内嵌该 Agent 的 TUI）。拖到列表是加入；拖到 AI 区是连同输入打进 TUI。
- 结果在工具内可点开查看；文件可拖到其他窗口，也可复制到剪贴板或从剪贴板贴入。剪贴板是进出通道，不是产品表面。
- 不是去解析、看懂或模拟操作任意终端画面。没有开着的会话时，拉起该 Agent 的 TUI。
- 六个 Recipe 在副本任务里跑，执行者是当前芯片那家 CLI（有 Workspace 才写 Workspace，否则按探测结果写未确认 / Safe Copy）。TUI 投递走终端自己的权限，界面必须写明「不是副本沙箱」。
- 不加载用户全局 MCP、Hooks、项目规则。
- 不自动覆盖原文件；不自动「应用修改到原件」。
- 第一版用 Developer ID 签名分发，不上 Mac App Store。

---

## 5. 做与不做

### 做

- 拖到菜单栏；拖文件时鼠标周围出现六瓣轮盘（加入架子、发给终端、总结、抽取、翻译、转 MD）。圆心是空的；拖出外圈轮盘消失。避开屏幕顶边标签栏。可在设置里关掉轮盘。
- 全局快捷键打开任务托盘。另一条全局快捷键把当前浏览器页面加入架子（网址 + 标题 + 正文 Markdown + 截图）。不装扩展。见第 12 节。第三条全局快捷键把前台选中的本地文件加入架子：Finder 读选中项；其他 App 先复制文件，没有文件再读当前打开的本地文件。设置里可改全部快捷键，并写清能拖什么、读什么、写到哪。
- 接收：文件、文件夹、PDF、图片、纯文本 / Markdown、URL、网站抓取（标题/链接/正文/截图）、多文件组合。
- 先暂存，再选 Recipe；也可多选后从 AI 区输入框发送到对应 Agent 的 TUI。
- 拖入时左右分区：左加入架子，右把这次材料（加框里的字）打进 TUI。菜单栏小图标仍是加入。
- 从剪贴板贴入；把结果或架子上的条目复制走或拖到别的窗口。规则见第 11 节。
- 不为某一家 App 做插件。拖出只交系统认的货（文件 / 文字 / 图 / 链接）。
- 每次任务用独立目录：`input/` 快照、`work/` 副本、`output/` 交付。
- 第一批：快捷动作和终端都跟芯片。预置 TUI：Grok / Claude / Gemini / OpenCode / Cursor CLI / Codex。预置纯 CLI：llm / aichat / sgpt。设置里可添加自定义 Runtime，芯片列表立刻出现。没有可收口 Job 入口的 TUI 不能跑动作；纯 CLI 用一条常规命令。
- 六个默认 Recipe（见第 6 节）：只列能力，第一版不开放任意 Shell 脚本。
- 结果只回到右栏结果区，由用户预览、复制、拖出。左列输入不被换成产出，还可以再跑。
- 执行前展示权限条：读什么、写什么、是否联网、当前是哪一档隔离。
- 执行中展示：工具调用、等待授权、失败原因。
- 执行后校验原件 Hash，确认未被 DropAgent 覆盖。

### 不做（第一版）

- 解析任意 TUI 画面，或把 DropAgent 做成通用终端遥控器。
- 任意自定义 Shell Action。
- 自动覆盖源文件；自动把结果写回原路径。
- 默认联网；默认继承用户全部环境变量。
- 默认加载用户所有 MCP 和 Hooks。
- 对未验证的自定义 CLI 宣称「严格沙箱」。
- 第一版就上 Container / VM。
- OCR、去背景、转录、AI 摘要当主功能（那是 Stash 的活）。
- 给没装 CLI 的人提供「开箱即用的云端模型」。
- 承诺搬运 Figma 图层、Photoshop 图层、Notion Block、时间线片段等私有对象。
- 为 Office / IM / AI 桌面单独做适配或自动点发送。

---

## 6. Recipe（仅名称与输入输出）

Recipe 是带权限和输入输出声明的执行单元，不是一个 Prompt 文本框。第一版只做六个高频、结果可检查的动作。Prompt 正文不在本文范围。

| Recipe | 接受 | 产出（新文件，不改原件） |
|--------|------|--------------------------|
| 总结文件 | PDF、图片、文本、URL | `summary.md` |
| 提取结构化信息 | PDF、图片、文本 | `extracted.json`（另附可读的 `extracted.md` 可选） |
| 翻译并保留格式 | 文本、Markdown、PDF | 同类型或 Markdown 的译稿 |
| 敏感信息脱敏 | 文本、Markdown、PDF | 脱敏后的新文件 |
| 转换为 Markdown | PDF、图片、网页 URL、文本 | `converted.md` |
| 把几份材料整合成一份 | 多文件组合 | 一个新文件（如 `brief.md` 或用户可见的单一输出） |

共同规则：

- 只向 `output/` 写最终交付。
- 不修改 `input/`。
- 「整理文件夹」若做，输出必须是**新的**整理后文件夹，不得移动或重排原路径中的文件。
- 第一版 Recipe 不允许内嵌任意 Shell。

---

## 7. 安全承诺怎么对外说

分三档，**不要都叫 Sandbox**。界面上必须写清当前任务实际是哪一档。

| 档位 | 用户看到的承诺 | 实际含义 | 第一版谁用 |
|------|----------------|----------|------------|
| **Safe Copy** | 原文件不会被 DropAgent 覆盖，结果另存 | Agent 处理副本；宿主进程理论上仍可能访问其他位置 | Claude Code；自定义 CLI |
| **Workspace Sandbox** | Agent 只能写任务工作区 | 使用该 Agent 官方提供的工作区写入限制 | Codex、Gemini CLI（以其官方能力为准，UI 按探测结果展示） |
| **Strict Isolation** | Agent 进程无法读取任务区之外的用户文件 | 整进程沙箱 / 容器 / 虚拟机 | **第一版不做** |

对外禁止说：

- 「完全看不到你的其他文件」——除非真的是 Strict Isolation。
- 把 Claude 的 Bash Sandbox 说成整进程隔离。

对所有任务都成立的工作流承诺（与隔离档位无关）：

- 原件复制进 `input/`，再复制到 `work/`。
- 不把原文件路径写进 Prompt。
- 不用指向原文件的软链接或硬链接。
- 跑完后校验原件 Hash。
- 结果先回右栏结果区，不自动覆盖原件，也不替换左列输入。

任务目录约定：

```text
Jobs/<id>/
  input/          原始快照，不允许修改
  work/           Agent 可修改的副本
  output/         最终输出，回到托盘的只有这里
  manifest.json   输入、来源、Hash、Recipe、Agent、权限档
  events.jsonl    过程记录
```

---

## 8. 原型范围

原型用来验证核心差异是否成立，不是第一版产品。

只验这四件事：

1. **拖入暂存**：把一个 PDF 或图片拖到菜单栏，出现在待处理列表，此时不跑 Agent。
2. **副本隔离**：选「总结文件」跑完后，原路径文件的 Hash 不变。
3. **芯片 CLI 执行**：使用当前芯片对应的本机 CLI；工作目录指向该任务的 `work/`；不加载用户全局配置。
4. **结果拖出**：`output/` 里出现新的 Markdown，能从菜单栏拖到 Finder 或桌面。

原型刻意不做：

- Claude / Gemini Adapter
- 六个 Recipe 全部落地（原型只做「总结文件」）
- Share Extension、屏幕边缘投放
- 自动发现所有安装路径（可先写死常见路径 + 手动指定可执行文件）
- Strict Isolation
- 任何「应用到原文件」

原型怎样算过：上述四条都能在本机亲手走通，并留下 Hash 对比记录。

---

## 9. 怎样算这个产品是成功的

不是「Agent 启动了」，而是：

- 用户离开原应用之后，仍能处理刚才拖入的内容。
- 原文件 Hash 没变。
- 菜单栏里出现新文件，能拖进 Finder、桌面、常见上传框，以及接受文件或文字的编辑器 / IM / AI 桌面输入区（见第 11 节）。
- 执行前用户能看懂：谁在跑、写哪里、是否联网、是哪一档隔离。
- 没装 Agent 时失败得清楚，而不是默默调云端模型。

---

## 10. 和邻近产品的边界

| 产品类型 | 代表 | DropAgent 不跟它抢什么 |
|----------|------|-------------------|
| 拖一下出动作 | Dropzone、FilePane | 不做成通用文件动作台 |
| AI 置物架 | Stash、Clippy | 不靠内置 OCR/去背景/模型按钮做人设 |
| Agent 启动器 | GroAsk、Carapace | 不负责盯着用户已有的项目 Session |
| 系统 AI 指挥中心 | EnConvo、Vehla | 不做成「从任意地方唤起 AI」 |

DropAgent 要成立，靠的是：**内容进 → 隔离 Job → 结果作为可拖拽 Artifact 出。**

---

## 11. 拖出落点（第一版）

拖出去交出系统认的货：真文件、一段字、一张图、或一个链接。不为某一家做插件。接不住就弹回，不弹错误窗。默认复制，架子上还在。多选时一次拖出所选项。还没跑的原件也能拖。

**保证：** Finder、桌面；接受文件的上传框、Office 附件、多数 IM 会话、多数 AI 桌面输入区；接受文字的编辑器 / 备忘录 / 聊天输入（货是文稿、总结或剪贴板时）。

**尽力：** 微信 Mac、个别套壳、Excel、没对准输入框的窗口。

**不做：** 写回原路径；自动点发送；Figma / PS / Notion 内部对象；Excel 自动成表、PPT 自动排版。

同一条同时带多种形态（有什么带什么）。细表以 `02-prototype-design.md` 第 6 节为准。

---

## 12. 快捷键添加当前网站（第一版）

在浏览器里按全局快捷键，把当前页加入架子。不装浏览器扩展。

**抓三样：** 网址 + 标题、正文转 Markdown、整页截图。架子上显示一条（`WEB` / 网站，标题作名字），内含这组材料。只暂存，不自动跑。

把 http(s) 拖到菜单栏图标、鼠标旁的轮盘、文件行，或粘贴整段网址：同样进一条网站条。先出现链接，后台补正文和截图（按该 URL 自己取页，不截 DropAgent 窗口）。拖到对话浮窗只把链接送进终端，不抓页。Safari 标签若系统不交出 URL，仍进不了货。

**浏览器：** 保证 Safari、Chrome；Edge 尽量。其他尽力，读不到当前页就明说。

**联网：** 抓正文由 DropAgent 自己访问该网址，要联网；失败时链接仍保留。第一版不承诺带上浏览器登录态去抓已登录正文。

**不是：** 阅读器、整站离线、浏览器插件。总结仍走 Recipe 或发给 Agent。

