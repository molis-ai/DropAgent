# DropAgent 原型设计

状态：设计原型（HTML），不是工程原型。  
场景：菜单栏面板。视觉对齐 Yoink / Dropover / 菜单栏开发工具，不是 AI 指挥中心。

## 1. 这张界面要完成的事

人已经在别的应用里拿到一份东西。打开 DropAgent，先放下，再决定要不要跑。跑完在左侧结果组看见新文件，再拖走或复制。左侧的材料仍在，原件不动。

第一眼必须看到：**架子上有什么。** 点「对话」才跟 Agent 说话。

## 2. 动线

当前交互以 `specs/linear-workbench/spec.md`（2026-09-12）为准，覆盖此前上下文件架与独立浮窗布局。

- 左侧目录顶上是拉宽的搜索框，右侧为添加与更多。搜索过滤架子上已有材料，满两字后也可添加本机文件。材料、生成结果、剪贴板历史都是可折叠目录名，文件相对目录缩进；结果有内容才出现，剪贴板默认展开最近 3 条，可查看全部 10 条。
- 单击材料或结果稳定选择并在右侧预览，重复单击不清空；Command 点或多选模式调整材料集合。双击系统打开。文件夹在左目录展开子项，右边预览选中的文件。
- 行可拖出；行尾按钮复制、隐藏、删除副本（剪贴板为复制、加入材料、删除），不用右键菜单。运行中不能隐藏或删除，⌫ 仍只隐藏当前材料或结果；剪贴板焦点下则删除所选历史。
- 从 Finder 把文件拖进打开的面板任意处，整块半透明罩显示「加入材料」，并提示发给终端走轮盘。松手加入副本。
- 右下方指令只列当前选中能用的动作，加上对话、整理、新建。对话与动作用分隔线分开。宽度不足时横向滚动。结果和剪贴板焦点下先用作材料／加入材料再处理。
- 参数、读取／写入／网络／隔离事实和开始按钮在指令上方出现；确认、运行、失败和取消均保留预览，窗口不因状态变化伸缩。
- 产出追加到左侧结果组，仍停在原任务材料时自动预览。切去其他材料、剪贴板或对话时不抢焦点。点击「对照原文」展开目录／原文／结果三栏；多个原材料可切换，窄窗口上下对照，缺失原材料有说明。
- 文字副本先阅读，右上角带纸面细边的「编辑副本」进入编辑；切换对象、Esc 或「完成编辑」保存并退出。对照原文只读，PDF 用 PDFKit。
- 对话／终端在右下方展开，与主工作台连接；收起、切换预览、进入设置保留 PTY。发送的终端边界说明保持可见。
- 剪贴板点选只预览，不改变系统剪贴板、不上架；复制切为当前，加入材料走 Ingest，URL 沿用抓页。外部复制本地文件仍自动进材料，不占历史。隐蔽／密码类型不记录，重复内容移到最前。
- 轮盘六瓣、中心空区、命中与离开消失规则不变，使用中性底色、细边与彩色图标，长标签换行。仍可在设置关闭。
- 顶栏空白可拖窗口，记住位置；菜单栏、全局快捷键、闲时降层与权限提示让位沿用现有行为。

## 3. 信息结构

| 区域 | 尺寸与内容 |
|---|---|
| 顶栏 | 48pt，品牌、当前位置、Agent、设置、最小化和关闭 |
| 左目录 | 213pt；顶部搜索框拉宽，右侧添加与更多；材料／结果／剪贴板为目录名，31pt 缩进行 |
| 右预览 | 随剩余空间伸展；结果按需原文对照，右侧不足 640pt 时上下对照 |
| 底部 | 确认／进度／错误在指令上方；对话按需展开；指令横排常驻 |
| 外窗 | 1160 × 640pt（含投影留白），受可用屏幕边界限制 |

## 4. 状态

| 状态 | 列表第二行 | AI 区 |
|------|------------|--------|
| 待处理 | 文档 · 待处理 | Recipe；跑完后仍是这一态 |
| 待确认 | 总结 · 未运行 | 权限四行 + 运行 |
| 运行中 | 运行中 | 最近事件 |
| 完成（旧数据） | 可拖出 | 结果正文；动作区指向拿走 |
| 失败（已有产出，旧数据） | 失败原因 | 仍可拿走；警告写在动作区 |
| 已进终端 | 已进终端 | 动作区仍是 Recipe；终端里继续说；结果页可回看材料 |
| 空 | 拖到这里 | 首次可直接放入示例 PDF 或选择文件；使用过后保留简单的拖入提示 |
| 无 TUI | 同待处理 | 发送禁用；没有芯片，动作也禁用 |
| 芯片 CLI 无执行入口 | 同待处理 | Recipe 禁用并说明原因；有 TUI 仍可发送 |

## 5. 视觉原则

当前视觉以 [DESIGN.md](DESIGN.md) 为准，执行边界见 [UI 与引导需求书](specs/ui-onboarding-refresh/spec.md)。

- 暖纸白／中性炭灰基底，雾蓝强调，类型与动作图标使用克制的灰蓝、麦色、灰紫、陶土色。原生系统字体，欢迎 26pt、页面 22pt、确认 20pt、正文 12–13pt。
- 主按钮在浅色主题中深雾蓝底暖白字、深色主题中浅雾蓝底深灰字，次按钮细边，图标按钮轻底；原生按钮键盘语义、可见焦点、hover / pressed / disabled。
- 首次直接进入真实样例路径：上架 PDF → 选择文字提取并确认 → 得到 pdf.md，原件不动。可跳过或选自己的文件；不前置 Agent 或授权。
- 文件卡显示文件名、摘要和类型；选择为淡雾蓝底与高对比细边。结果区能直接辨认新文件名、复制或拖走。
- 设置为用途导航 + 当前页标题说明；分段选择底片平滑移动。对话有输入边界、发送文字和收起按钮。
- 参考 Animate UI 的状态反馈，以 SwiftUI 原生实现；动效 180–300ms，尊重减少动态效果。

## 6. 拖出：落到哪里、对方拿到什么

拖出去不是遥控对方 App，而是交出一份**系统认的货**：真文件、一段字、一张图、或一个链接。对方爱接哪种用哪种。接不住就弹回架子，不弹错误窗。架子上默认还在（复制，不是挪走）。从架子拖起再松在文件行上，只是放下，不复制第二条。多选时一次拖出所选项。还没跑的原件也能拖。

同一条会**同时带几种形态**（有文件带文件，有字带字，有链接带链接），让对方自己挑。不为某一家做插件。

### 按货看对方拿到什么

| 你拖的是 | Finder / 桌面 | 上传框 | 文本框（编辑器、聊天输入） |
|----------|---------------|--------|----------------------------|
| 跑完的结果（如 `summary.md`） | 落下新文件 | 当附件 | 能吃字就贴正文 |
| 还没跑的原件（PDF / 图） | 再交一份复制，不挪原件 | 同上 | 图有的能贴；PDF 多数当文件 |
| 链接 | 不落成文件 | 对方只要文件则接不住 | 贴成 URL |
| 网站 | 落下文件夹：链接、正文 md、截图 | 能吃多文件就交一组；否则优先 md | 文本框贴正文；只要图则交截图 |
| 剪贴板那条 | 字则落成 `.txt`；若是文件仍是文件 | 看对方要文件还是字 | 贴成字 |

### 第一版保证

- Finder、桌面
- 接受**文件**的地方：网页上传框、Office 附件、多数 IM 会话、多数 AI 桌面的输入区
- 接受**文字**的地方：文本编辑器、备忘录、聊天输入（货必须是文稿 / 总结 / 剪贴板）

### 各品类

**浏览器**

- 对准「选择文件 / 拖到这里上传」：和从 Finder 拖进去一样。
- 结果是一篇字时：同时带文件 + 纯文本。
- 普通网页、地址栏、不接受拖放的按钮：不保证，弹回。

**文本编辑器**（备忘录、TextEdit、VS Code、Typora 等）

- 文稿 / 总结 / 剪贴板：落下是字。
- PDF、图片：由编辑器自己决定当文件、打开、还是不接。

**Office**

| 软件 | 拖文稿 | 拖 PDF / 图 | 稳不稳 |
|------|--------|-------------|--------|
| Word | 正文区多半当文字；有的当嵌入文件 | 图能插图；PDF 常成文件图标 | 文稿 + 图比较稳 |
| PowerPoint | 要对准文本框才像贴字 | 图当页上的图 | 图稳，长文一般 |
| Excel | 字可能进单元格，版式别指望 | 图能放 | 不当主场景 |
| Outlook 写信 | 当附件 | 附件或插图 | 稳 |

不承诺：Excel 自动成表、PPT 自动分页排版。

**IM**

| 类型 | 典型 | 行为 |
|------|------|------|
| Slack、飞书、Discord、Telegram、iMessage | 拖到输入区 / 会话 | 当附件或图片；有的松手后还要再点发送 |
| 微信 Mac | 有时吃文件/图 | 尽力而为，不写进保证 |
| 只吃字的输入框 | 拖总结、剪贴板 | 当字贴进去 |

聊天里最稳的是图片和文件。长 Markdown 有的会变成附件而不是一条消息。

**AI 桌面**（ChatGPT 桌面、Claude 桌面、Cursor、各类套壳）

- 多数和上传框同类：拖到对话框当附件，或把正文贴进输入框。
- 没对准输入区、点在空白窗口：可能弹回。
- 不承诺变成对方内部的项目 / 知识库 / 画布节点。

### 尽力、接不住就弹回

微信、个别 Electron 套壳、Excel、没对准输入框的窗口。

### 不做

- 为某个软件单独适配或做插件
- 写回原路径、自动帮你点发送
- Figma / Photoshop / Notion 内部对象

## 7. 快捷键添加当前网站

人正在 Safari / Chrome（以及同源的 Edge）里看一页时，按全局快捷键，当前页进入架子。**不装浏览器扩展。** 具体按键以后定；和「打开托盘」是两条快捷键。

**抓什么（都要）：**

1. 网址 + 标题  
2. 正文，转成 Markdown（`page.md`）  
3. 整页截图（`snapshot.png`）

架子上显示**一条**，标签 `WEB`，类型字「网站」，名字用页面标题。点开能看链接、正文、截图。这是一组材料，发给当前 TUI 或跑「整合」时三份都能带上。

把链接拖进材料（打开的面板任意处 / 图标 / 轮盘「加入材料」）或粘贴整段 `https://…`：走同一条网站条。先出现条目，再补正文和截图；截图按该 URL 单独加载，不截前台窗（避免面板抢到前台后拍到自己）。轮盘「发给终端」仍是链接，发给当前终端，不抓页。

**怎么拿到当前页：** 快捷键读最前面那个浏览器窗口的地址和标题。第一版保证 Safari、Chrome；Edge 尽量带上。Arc、Firefox 等尽力，读不到就提示「没读到当前页」，不要假成功。拖入/粘贴已经带着 URL，不必再读前台浏览器。Safari 把标签拖成窗口管理、剪贴板没有 URL 时，进不了货，也不偷偷抓前台页。

**抓正文和截图：** DropAgent 自己打开这个网址去抓（要联网）。这不是 Agent 任务，也不走副本沙箱那套说辞。登录墙、验证码、纯脚本页：链接一定留下；正文或截图失败就在该条上写「正文未抓到」或「截图未抓到」，其余成功的仍可用。

**加进来之后：** 只进架子，不自动跑 Recipe、不自动发给终端。没装 Agent 也可以抓。

第一次打开面板：空架子和欢迎区同时可见，直接选择示例 PDF、自己的文件或跳过。示例无需 Agent 或权限，可完成本机提取和结果导出。抓页、Finder 权限在需要时申请，完整清单在设置「权限与连接」；设置「使用指南」可再次尝试样例。授权仍一次一个系统对话框，不同时打开空的自动化页，辅助功能身份不匹配时才转系统设置。

设置页（齿轮，盖住文件架）：左右分栏。左是使用准备、快捷键、能做什么、本机、外观。右是该类内容。使用准备是终端 Agent、辅助功能、Finder、已装浏览器；快捷键占用改在「快捷键」里改。本机页可输入命令名（如 `kimi`）或选择可执行文件，添加自定义 Runtime。抓页准备好 = 辅助功能已开且至少一家浏览器已允许，不要求所有已装浏览器都授权。全局快捷键必须带修饰键。外观里可关拖放轮盘。轮盘不自动开合面板。原件不覆盖。

**不是：** 阅读器产品、离线整站下载、浏览器扩展、给每个网站做登录态同步。用的是你当时那个浏览器里已经打开的地址，抓正文时按这个地址重新取页，不一定带上你的登录 Cookie（第一版不承诺已登录正文）。若正文因未登录是空的，截图仍尽量用当前窗口（能取到前台窗口画面就取，取不到再单独加载该 URL）。

可点原型：`prototype/index.html`（桌面井只演示「拖到别的窗口」，不是完整落点清单）。评审场景含空、暂存、多选、终端、结果、无终端、仅终端（有 TUI、该 CLI 无执行入口）。



## 8. 原生交互打磨（2026-09-08）

此段旧布局已由第 2–3 节与 `specs/linear-workbench/spec.md` 替代；其余文件处理规则继续适用。

按钮沿用系统点击与键盘激活；鼠标按下后移出再松开不执行。展开「对话」定位到输入框；终端视图不随 Tab 切换销毁。减少动态效果时禁用非必要过渡，不闪烁整行文字。

新增材料把当前对象切回输入；结果列表的方向键留在结果列表。没有文件的失败记录显示失败原因和「返回材料重试」，不提供复制或拖出。准备副本失败恢复条目状态；同一执行器一次只运行一项任务。首次示例融入工作栏，已用过后不重复出现。


## 9. 第二轮原生打磨 · 2026-09-08

2026-09-11 用户试用纯黑白后反馈视觉疲劳，最新配色以宁静、适合效率工具为目标：雾蓝主色 `#586582`、暖纸白面板 `#f8f7f4`，深色主题使用中性炭灰与浅雾蓝主操作，文件和动作图标有固定的低饱和色组，遵循用户最新要求，不使用绿色。当前配色执行见 `specs/calm-color/spec.md`；此前的文案精简仍见 `specs/monochrome-copy/spec.md`。

动作整行默认无卡框，悬停时显示底色；选项采用清楚的勾选格。确认页增加动作标题、当前材料和返回入口。页签分段底片移动，动作状态轻微淡入，终端实例不重建。减少动态效果时不做位移动画。

点「动作」明确切回输入对象；无输入选择时点「预览」打开已有结果。隐藏当前结果后选择相邻结果。键盘切换列表时同步滚动。切换到其他材料后，可从「运行中」返回任务并取消。新动作确认替换旧确认，不把不同动作的材料静默混跑。
