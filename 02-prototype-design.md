# DropAgent 原型设计

状态：设计原型（HTML），不是工程原型。  
场景：菜单栏面板。视觉对齐 Yoink / Dropover / 菜单栏开发工具，不是 AI 指挥中心。

## 1. 这张界面要完成的事

人已经在别的应用里拿到一份东西。打开 DropAgent，先放下，再决定要不要跑。跑完在文件下方的结果区看见新文件，再拖走或复制。上面的文件还在，原件不动。

第一眼必须看到：**架子上有什么。** 点「其他」才跟 Agent 说话。

## 2. 动线

面板不跳页。上面是文件，选中后才出动作；下面是结果区（没结果则不出现）；点「其他」才在面板下方弹出独立对话浮窗。人去别的 App 干活时，开着的面板变淡并沉到后面，不挡住阅读和点击；点还露着的面板，或把文件拖进原来那块区域，再回到最前。菜单栏图标在闲时是唤醒，已经亮着再点才关。

**上：文件（横向）**

- 拖到菜单栏图标、鼠标旁的轮盘、或列表区域 = 加入。轮盘钉在指针周围，六瓣：加入架子、发给终端、总结、抽取、翻译、转 MD。圆心是空的；拖出外圈轮盘消失。屏幕最顶标签栏那一带不出现轮盘。可在设置 → 外观关掉轮盘；关掉后图标和列表仍接拖。
- Command 点多选；列表顶有「多选」，进入后行前勾选。再点已选中的文件或结果，取消选中。双击用系统默认方式打开：文件和结果打开架子上的副本，链接打开浏览器。类型用标签（PDF / PNG / URL / MD / CLIP / WEB / DIR / FILE 或 1–4 位扩展名）+ 字（文档 / 图片 / 链接 / 文稿 / 剪贴板 / 网站 / 文件夹 / 文件）。
- 整行可拖到其他窗口。拖出规则见第 6 节。
- 每行叉 = 隐藏（只从列表拿掉，Inbox 文件还在）；垃圾桶 = 删除 DropAgent 里的副本，原件不动。运行中两项都不能用。⌫ 是隐藏。
- 浏览器里按全局快捷键：把当前页加入架子。见第 7 节。
- 在 Finder 或其他 App 里按全局快捷键：把选中的本地文件加入架子。Finder 读选中项；编辑器先复制文件，没有文件再读当前打开的文件。浏览器最前请用抓页。

**动作栏（选中文件后才出现）**

- 六个 CLI Recipe + 「其他」横排；选中项都是图片时多一个「文字提取」。点 Recipe 后确认条带选项（翻译选目标语言且源语言自动识别、抽取选抽什么、总结选篇幅、文字提取选语言），默认写进命令。CLI Recipe 用当前引擎在副本里跑，没有可收口执行入口则禁用。「文字提取」用本机 Vision，不需要终端 Agent。
- 「其他」在文件架下方弹出独立浮窗发给终端，不和文件架连成一块。已进终端的条目动作栏仍在。
- 终端：浮窗里内嵌 PTY。底色跟外观：浅色白底，深色黑底。纯 CLI 在默认 shell 里发一条普通命令。
- 预览：悬停文件或结果看正文，出现在整个面板左侧。Markdown 按标题、列表、表格画；HTML 抽成可读正文再画，不是网页。离开卡片后预览再留约两秒，方便移过去；鼠标在预览卡内不消失，过长正文可上下滚，宽表和长代码可左右滑。点结果也可在浮窗看。
- 拖入浮窗高亮「发给」当前终端引擎名。

**下：结果区（有结果才出现）**

- Job 产出追加在这里，上面的文件不变。
- 卡片可拖到桌面 / 别的窗口。选中后「放到上面当材料」复制进文件行，不是挪走，也不是覆盖原件。再点取消选中；双击打开。
- 每张同样可隐藏 / 删除 DropAgent 里这份产出所在任务目录。
- 旧 `done` 行仍走拿走。

**剪贴板**

- ⌘V：把**当前**剪贴板加成一条。对话浮窗不放粘贴按钮。
- 文件行上的粘贴按钮：在按钮旁展开和面板同一套纸面的下拉（系统剪贴板 + 本 App 复制过的），关面板后还在。行上是摘要；圆点标出此刻系统剪贴板那条。点选 / 多选，选中后拖到架子或拖出，点击不直接进货。可删除。满了丢掉最旧；相同内容提到最前。隐蔽 / 密码类不记。
- 复制：结果进系统剪贴板，从而进入这份历史。
- 不是剪贴板管理器，没有历史列表当首页。

## 3. 信息结构（约 1040px 面板）

| 层 | 尺寸 | 放什么 |
|----|------|--------|
| 头 | 48px 通栏 | 左字标；搜索本机文件（下拉结果，随可用宽度收缩）；右：终端芯片、设置、最小化、关闭 |
| 文件 | 一行横向卡片；不能关 | 「文件」+ 多选；文件卡（可含正文片段）；选中后动作栏 |
| 结果区 | 一行横向卡片；无结果则隐藏 | 「结果区」+ 多选；选中后「放到上面当材料」、复制、终端 |
| 对话浮窗 | 文件架下方独立面板，12px 圆角与投影同文件架 | 点「其他」才出现；「对话」分区条 + 终端卡片 + 发送 |

## 4. 状态

| 状态 | 列表第二行 | AI 区 |
|------|------------|--------|
| 待处理 | 文档 · 待处理 | Recipe；跑完后仍是这一态 |
| 待确认 | 总结 · 未运行 | 权限四行 + 运行 |
| 运行中 | 运行中 | 最近事件 |
| 完成（旧数据） | 可拖出 | 结果正文；动作区指向拿走 |
| 失败（已有产出，旧数据） | 失败原因 | 仍可拿走；警告写在动作区 |
| 已进终端 | 已进终端 | 动作区仍是 Recipe；终端里继续说；结果页可回看材料 |
| 空 | 拖到这里 | 首次使用显示三步说明和示例文稿；使用过后提示选文件或发给当前 TUI |
| 无 TUI | 同待处理 | 发送禁用；没有芯片，动作也禁用 |
| 芯片 CLI 无执行入口 | 同待处理 | Recipe 禁用并说明原因；有 TUI 仍可发送 |

## 5. 视觉原则

气质对齐 GoalBoard Onboarding：留白、引导、描边图标、不打扰。不是把全屏引导页塞进菜单栏。

- 浅纸面（`#fafbf9`）与灰绿侧栏（`#eff2ee`）挂在夜色桌面上。不用钴蓝当品牌色。选中用浅纸面和低饱和绿描边，不用色条。
- 文件种类用小图标 + 色标签区分，不给整卡上色，不用绿色。灰尘色：PDF 褐、图片蓝、链接蓝灰、网站青灰、MD 靛、剪贴板赭、文件夹暖石。标签上同时有图标和字母，不单靠颜色。
- 中间是工具不是问卷。顶上三个 Tag（动作 / 终端 / 预览）分开留白，选中浅底。六个 Recipe 和「其他」仍是纵列，每行图标、标题和说明整体可点。底下输入框默认收着，点「其他」才在原地展开，不是弹窗。发给当前终端，不是副本沙箱。主操作实心深绿，次要几乎无底。
- 系统字体 + PingFang。字标 13px 系统半粗体。动作、发送用 13px 描边图标。
- 控件尽量无框。发送是箭头，运行是文字按钮。运行、发送和「拖出」使用实心深绿。
- 空状态用一句标题 + 一句说明来引导，不要大图标海报。
- 不做玻璃，不做发丝网格当结构。层次靠深浅，分割条默认看不见。
- 面板圆角 12px，选项行 6px。

## 6. 拖出：落到哪里、对方拿到什么

拖出去不是遥控对方 App，而是交出一份**系统认的货**：真文件、一段字、一张图、或一个链接。对方爱接哪种用哪种。接不住就弹回架子，不弹错误窗。架子上默认还在（复制，不是挪走）。从架子拖起再松在文件行上，只是放下，不复制第二条。多选时一次拖出所选项。还没跑的原件也能拖。

同一条会**同时带几种形态**（有文件带文件，有字带字，有链接带链接），让对方自己挑。不为某一家做插件。

### 按货看对方拿到什么

| 你拖的是 | Finder / 桌面 | 上传框 | 文本框（编辑器、聊天输入） |
|----------|---------------|--------|----------------------------|
| 跑完的结果（如 `summary.md`） | 落下新文件 | 当附件 | 能吃字就贴正文 |
| 还没跑的原件（PDF / 图） | 再交一份复制，不挪原件 | 同上 | 图有的能贴；PDF 多数当文件 |
| 链接 | 不落成文件 | 对方只要文件则接不住 | 贴成 URL |
| 网站 | 落下文件夹：链接、正文 md、截图 | 能吃多文件就交一组；否则优先 md | 文本框贴正文；只要图则交截图 |
| 剪贴板那条 | 字则落成 `.txt`；若是文件仍是文件 | 看对方要文件还是字 | 贴成字 |

### 第一版保证

- Finder、桌面
- 接受**文件**的地方：网页上传框、Office 附件、多数 IM 会话、多数 AI 桌面的输入区
- 接受**文字**的地方：文本编辑器、备忘录、聊天输入（货必须是文稿 / 总结 / 剪贴板）

### 各品类

**浏览器**

- 对准「选择文件 / 拖到这里上传」：和从 Finder 拖进去一样。
- 结果是一篇字时：同时带文件 + 纯文本。
- 普通网页、地址栏、不接受拖放的按钮：不保证，弹回。

**文本编辑器**（备忘录、TextEdit、VS Code、Typora 等）

- 文稿 / 总结 / 剪贴板：落下是字。
- PDF、图片：由编辑器自己决定当文件、打开、还是不接。

**Office**

| 软件 | 拖文稿 | 拖 PDF / 图 | 稳不稳 |
|------|--------|-------------|--------|
| Word | 正文区多半当文字；有的当嵌入文件 | 图能插图；PDF 常成文件图标 | 文稿 + 图比较稳 |
| PowerPoint | 要对准文本框才像贴字 | 图当页上的图 | 图稳，长文一般 |
| Excel | 字可能进单元格，版式别指望 | 图能放 | 不当主场景 |
| Outlook 写信 | 当附件 | 附件或插图 | 稳 |

不承诺：Excel 自动成表、PPT 自动分页排版。

**IM**

| 类型 | 典型 | 行为 |
|------|------|------|
| Slack、飞书、Discord、Telegram、iMessage | 拖到输入区 / 会话 | 当附件或图片；有的松手后还要再点发送 |
| 微信 Mac | 有时吃文件/图 | 尽力而为，不写进保证 |
| 只吃字的输入框 | 拖总结、剪贴板 | 当字贴进去 |

聊天里最稳的是图片和文件。长 Markdown 有的会变成附件而不是一条消息。

**AI 桌面**（ChatGPT 桌面、Claude 桌面、Cursor、各类套壳）

- 多数和上传框同类：拖到对话框当附件，或把正文贴进输入框。
- 没对准输入区、点在空白窗口：可能弹回。
- 不承诺变成对方内部的项目 / 知识库 / 画布节点。

### 尽力、接不住就弹回

微信、个别 Electron 套壳、Excel、没对准输入框的窗口。

### 不做

- 为某个软件单独适配或做插件
- 写回原路径、自动帮你点发送
- Figma / Photoshop / Notion 内部对象

## 7. 快捷键添加当前网站

人正在 Safari / Chrome（以及同源的 Edge）里看一页时，按全局快捷键，当前页进入架子。**不装浏览器扩展。** 具体按键以后定；和「打开托盘」是两条快捷键。

**抓什么（都要）：**

1. 网址 + 标题  
2. 正文，转成 Markdown（`page.md`）  
3. 整页截图（`snapshot.png`）

架子上显示**一条**，标签 `WEB`，类型字「网站」，名字用页面标题。点开能看链接、正文、截图。这是一组材料，发给当前 TUI 或跑「整合」时三份都能带上。

把链接拖进架子（图标 / 轮盘「加入架子」 / 文件行）或粘贴整段 `https://…`：走同一条网站条。先出现条目，再补正文和截图；截图按该 URL 单独加载，不截前台窗（避免面板抢到前台后拍到自己）。拖到对话浮窗或轮盘「发给」仍是链接，发给当前终端，不抓页。

**怎么拿到当前页：** 快捷键读最前面那个浏览器窗口的地址和标题。第一版保证 Safari、Chrome；Edge 尽量带上。Arc、Firefox 等尽力，读不到就提示「没读到当前页」，不要假成功。拖入/粘贴已经带着 URL，不必再读前台浏览器。Safari 把标签拖成窗口管理、剪贴板没有 URL 时，进不了货，也不偷偷抓前台页。

**抓正文和截图：** DropAgent 自己打开这个网址去抓（要联网）。这不是 Agent 任务，也不走副本沙箱那套说辞。登录墙、验证码、纯脚本页：链接一定留下；正文或截图失败就在该条上写「正文未抓到」或「截图未抓到」，其余成功的仍可用。

**加进来之后：** 只进架子，不自动跑 Recipe、不自动发给终端。没装 Agent 也可以抓。

第一次打开面板：若辅助功能或已装浏览器的控制权还没齐，叠一张「使用准备」清单。可点「以后再说」。同一张清单在设置里最上面。点授权必须弹出系统控制对话框，不能只打开空的自动化设置页。

设置页（齿轮，盖住文件架）：左右分栏。左是使用准备、快捷键、能做什么、本机、外观。右是该类内容。使用准备是终端 Agent、辅助功能、Finder、已装浏览器；快捷键占用改在「快捷键」里改。抓页准备好 = 辅助功能已开且至少一家浏览器已允许，不要求所有已装浏览器都授权。全局快捷键必须带修饰键。外观里可关拖放轮盘。轮盘不自动开合面板。原件不覆盖。

**不是：** 阅读器产品、离线整站下载、浏览器扩展、给每个网站做登录态同步。用的是你当时那个浏览器里已经打开的地址，抓正文时按这个地址重新取页，不一定带上你的登录 Cookie（第一版不承诺已登录正文）。若正文因未登录是空的，截图仍尽量用当前窗口（能取到前台窗口画面就取，取不到再单独加载该 URL）。

可点原型：`prototype/index.html`（桌面井只演示「拖到别的窗口」，不是完整落点清单）。评审场景含空、暂存、多选、终端、结果、无终端、仅终端（有 TUI、该 CLI 无执行入口）。



## 8. 原生交互打磨（2026-09-08）

以上下架为基线。文件与结果是横向卡片；选中或悬停时显示隐藏。文件行有粘贴和添加。结果为空则不出现结果区。点「其他」才出对话浮窗。

按钮沿用系统点击与键盘激活；鼠标按下后移出再松开不执行。展开「其他」定位到输入框；终端视图不随 Tab 切换销毁。减少动态效果时禁用非必要过渡，不闪烁整行文字。

新增材料把当前对象切回输入；结果列表的方向键留在结果列表。没有文件的失败记录显示失败原因和「返回材料重试」，不提供复制或拖出。准备副本失败恢复条目状态；同一执行器一次只运行一项任务。首次示例融入工作栏，已用过后不重复出现。


## 9. 第二轮原生打磨 · 2026-09-08

用户明确要求不用绿色。当前配色改为冷白 `#fcfcfd`、石墨灰和蓝色 `#365cda`；覆盖第 8 节旧绿配色，包括文件标签、运行状态和深色主题。

动作整行默认无卡框，悬停时显示底色；选项采用清楚的勾选格。确认页增加动作标题、当前材料和返回入口。页签分段底片移动，动作状态轻微淡入，终端实例不重建。减少动态效果时不做位移动画。

点「动作」明确切回输入对象；无输入选择时点「预览」打开已有结果。隐藏当前结果后选择相邻结果。键盘切换列表时同步滚动。切换到其他材料后，可从「运行中」返回任务并取消。新动作确认替换旧确认，不把不同动作的材料静默混跑。
