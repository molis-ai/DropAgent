# 顶边进货不开关面板

## 背景与目标

拖到顶边黑条会进架子，但同时把面板弹开。人只想先放下，展开收起由自己点菜单栏图标或快捷键。

## 范围

- `admitFromEdge` 只 `admitPayload` 并结束拖拽层，不 `showPanel`，也不 `hidePanel`。
- 面板当时开着就还开着，关着就还关着。
- 顶边投放条本身松手后仍收起。
- 菜单栏图标点击、热键、双击 App 仍按原规则开关面板。

## 验收

1. Finder 拖文件到顶边黑条：架子多一条，面板不因此打开或关掉。
2. Check 全绿；e2e 通过。
