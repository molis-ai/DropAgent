---
name: DropAgent
description: Native menu-bar shelf in monochrome: precise, quiet, and functional.
colors:
  panel: "#fcfcfc"
  sidebar: "#f3f3f3"
  hover: "#e9e9e9"
  selection: "#e5e5e5"
  text: "#1a1a1a"
  muted: "#5b5b5b"
  accent: "#171717"
  dark-panel: "#191919"
  dark-sidebar: "#232323"
  dark-accent: "#f5f5f5"
---

# 当前视觉约定

2026-09-11：黑白界面与简洁文案。当前视觉见 [需求书](specs/monochrome-copy/spec.md)，首次路径沿用 [onboarding 需求书](specs/ui-onboarding-refresh/spec.md)。

DropAgent 是菜单栏小工具：材料在上、内容在中、新文件在下，对话独立展开。纯中性白与石墨灰构成工作面；主操作使用黑白反差，选择使用灰底、细边和字重。深浅主题都不使用彩色强调。简约科技感来自精确的排版和反馈，不使用发光、装饰网格或渐变。

- 参考 [Animate UI](https://github.com/imskyleen/animate-ui) 的按钮反馈与选中底片，以原生 SwiftUI 实现。它是 React 组件库，本项目未引入 React 或 WebView 组件层。
- 首次打开直接展示价值和三步路径：添加材料 → 选择动作 → 导出结果。主按钮为“试用示例 PDF”，另可选择文件或跳过。权限留到按需设置。
- 示例是真实 PDF；上架后引导选择本机提取，确认后才运行。结果区引导复制或拖走。跳过不紧接着弹权限提醒；设置 → 使用指南可以重新试用。
- 系统字体 SF Pro / PingFang。欢迎标题 26pt、页面标题 22pt、确认标题 20pt、正文 12–13pt、辅助说明 11–12pt。正文留行距，不用等宽字体装饰分区标题。
- 主按钮浅色主题为深底白字，深色主题为浅底黑字；次要按钮纸面细边；图标和轻操作默认透明。控件约 30–36pt 高、8pt 圆角。悬停改变底色，按下短弹簧收缩，禁用明确降对比；键盘焦点用高对比中性轮廓。
- 文件卡显示文件名、内容摘要和类型标签，结果的文件名可直接辨认。灰底与细边表示选择；悬停有底色反馈。卡片用边框，不叠加投影。
- 内容台有对象标题；PDF 用原生 PDFKit 阅读，文字内容可点击“编辑副本”。确认和运行时收起内容台，优先展示执行范围和取消入口。
- 动作栏先列实际动作，再列对话、整理、新建动作。查看结果时隐藏原材料动作，先“用作材料”再继续处理结果。
- 确认页显示动作名、材料数、将生成的文件名、选项、读取／写入／网络／隔离事实、执行与返回按钮。隔离如实表述。
- 设置左侧按用途导航，右侧有标题和说明。分段选择为真实按钮；选中底片用短弹簧移动。轻表单背景建立分组，避免层层描边。
- 对话浮窗保留原生终端实例；输入区域有明确边界和焦点，发送有文字，顶部可收起。系统授权与重试错误必须可见。
- 动效 180–300ms，服务于 hover、pressed、选择与展开；减少动态效果时关闭位移与缩放。
- 主面板约 1040pt 宽、高度随内容；外层 12pt 圆角和单层柔和投影。英文和 800pt 预览采用可换行布局，不压缩成小字。

文案以对象、动作、结果为中心，使用“添加材料”“提取文字”“开始提取”“新文件已生成”“复制文件”“用作材料”。同一功能用同一名称；先写当前事实与可执行动作，减少口号、口语赘词、重复解释。中文与英文同步。文件类型依靠图标和标签区分，状态依靠文字与符号表达，不依赖颜色。文件内容及第三方终端输出保留自身颜色。
