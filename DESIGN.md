---
name: DropAgent
description: A calm native menu-bar shelf with warm surfaces, mist-blue accents, and restrained icon color.
colors:
  panel: "#fcfcfb"
  sidebar: "#f5f5f4"
  hover: "#eeeeee"
  selection: "#e8e9ee"
  text: "#292a2e"
  muted: "#74757d"
  accent: "#66709e"
  dark-panel: "#19191b"
  dark-sidebar: "#111112"
  dark-accent: "#a6afd5"
  dark-text: "#e9e9ed"
  dark-muted: "#96969f"
  line: "#e8e8e6"
  dark-line: "#2b2b2f"
---

# 当前视觉约定

2026-09-12：采用用户确认的 [Linear 风格文件工作台与轮盘](specs/linear-workbench/spec.md)。文案沿用 [简洁文案](specs/monochrome-copy/spec.md)，首次路径沿用 [onboarding 需求书](specs/ui-onboarding-refresh/spec.md)。

DropAgent 是菜单栏小工具：左侧目录分材料、生成结果、剪贴板历史，右侧预览，指令常驻右下。结果可按需对照原文，对话在预览下方展开。近白与中性炭灰构成连续工作面；雾蓝用于主操作、选择和焦点。彩色集中在小图标与类型标签，正文保持中性，不使用绿色、发光、装饰网格或渐变。

固定图标色组：雾蓝对应文字、处理与完成；钢蓝对应网页、代码、信息提取与翻译；麦色对应文件夹、快捷动作与待处理提醒；灰紫对应图片、对话与文稿转换；陶土对应 PDF 与错误提示。错误另有警示符号和原因，不只靠颜色。图标和底色均不使用绿色；浅深主题分别配置墨色与淡底，禁用图标回到辅助文字色。

- 参考 [Animate UI](https://github.com/imskyleen/animate-ui) 的按钮反馈与选中底片，以原生 SwiftUI 实现。它是 React 组件库，本项目未引入 React 或 WebView 组件层。
- 首次打开直接展示价值和三步路径：添加材料 → 选择动作 → 导出结果。主按钮为“试用示例 PDF”，另可选择文件或跳过。权限留到按需设置。
- 示例是真实 PDF；上架后引导选择本机提取，确认后才运行。结果区引导复制或拖走。跳过不紧接着弹权限提醒；设置 → 使用指南可以重新试用。
- 系统字体 SF Pro / PingFang。欢迎标题 26pt、页面标题 22pt、确认标题 15pt、正文 12–13pt、辅助说明 11–12pt。正文留行距，不用等宽字体装饰分区标题。
- 主按钮浅色主题为深雾蓝底、暖白字，深色主题为浅雾蓝底、深灰字；次要按钮纸面细边；图标和轻操作默认透明。控件约 30–36pt 高、8pt 圆角。悬停改变底色，按下短弹簧收缩，禁用明确降对比；键盘焦点用高对比雾蓝轮廓。
- 目录用 31pt 紧凑行显示彩色小图标和文件名，淡灰蓝底表示选择，悬停显示轻底色；不加卡片边框与投影。材料、生成结果、剪贴板均为可折叠目录名，文件相对目录缩进一级。悬停或选中时行尾出现复制、加入材料、隐藏、删除按钮，不用右键菜单。
- 顶栏 48pt，品牌区与 213pt 目录对齐。目录顶部搜索框拉宽，右侧为添加与更多操作。右侧内容面内缩 7pt、9pt 圆角、1pt 细边；正文水平留白 42pt、顶部 35pt，对照时缩为 22pt / 24pt。
- 内容台有对象标题；PDF 用原生 PDFKit 阅读，文字内容可点击“编辑副本”。确认和运行始终保留预览，执行范围和取消入口在底部指令上方。原文对照只读，结果仍可编辑。
- 动作栏先列实际动作；对话固定在右侧，末尾菜单提供整理与新建动作。结果或剪贴板焦点下材料动作仍在但禁用，先“用作材料”或“加入材料”再处理。
- 确认页显示动作名、材料数、将生成的文件名、选项、读取／写入／网络／隔离事实、执行与返回按钮。隔离如实表述。
- 设置左侧按用途导航，右侧有标题和说明。分段选择为真实按钮；选中底片用短弹簧移动。轻表单背景建立分组，避免层层描边。
- 右下嵌入式对话保留原生终端实例；输入区域有明确边界和焦点，发送有文字，顶部可收起。系统授权与重试错误必须可见。
- 动效 180–300ms，服务于 hover、pressed、选择与展开；减少动态效果时关闭位移与缩放。
- 主面板外框约 1160 × 640pt（含投影留白），高度不随任务状态变化，适配可用屏幕。外层 12pt 圆角与柔和投影；800pt 窄预览上下对照，指令可横向滚动。
- 剪贴板历史默认展开最近 3 条，最多 10 条；点击只预览，复制与加入材料是明确操作。当前记录有文字标记。
- 轮盘保持六瓣与中心空区、拖放命中和离开消失规则；中性实底、细边、彩色图标，悬停用浅色底与描边，长标签换行。

文案以对象、动作、结果为中心，使用“添加材料”“提取文字”“开始提取”“新文件已生成”“复制文件”“用作材料”。同一功能用同一名称；先写当前事实与可执行动作，减少口号、口语赘词、重复解释。中文与英文同步。文件类型依靠图标和标签区分，状态依靠文字与符号表达，不依赖颜色。文件内容及第三方终端输出保留自身颜色。
